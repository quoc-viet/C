#include "stdio.h"

int fabonacci(int x){
	if (x==0)
		return 0;
	else if (x==1)
		return 1;
	else if (x==2)
		return 1;
	else 
	   return fabonacci(x-1)+fabonacci(x-2);
}


int main(){
	int n;
	do
	{
		printf("Nhap n = ");
		scanf("%d", &n);
	}while(n<0);
	
	printf("fabonacci(%d)=%d", n, fabonacci(n));
	printf("\n");
	fibo(n);
	
	}
